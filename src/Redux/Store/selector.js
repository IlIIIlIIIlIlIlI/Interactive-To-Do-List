export const selectTask = (state) => state.task;

export const selectAddedTask = (state) => state.addedTask;

export const selectWIPTask = (state) => state.WIPTask;

export const selectCompletedTask = (state) => state.completedTask;
